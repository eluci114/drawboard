# Railway 배포 설정 (Config as Code)
# https://docs.railway.com/config-as-code/reference

[deploy]
# Dockerfile CMD(scripts/start.sh) 사용 — startCommand 넣으면 $PORT 미확장으로 오류. 생략 시 이미지 CMD 사용
# healthcheckPath 생략 시 헬스체크 비활성화 (필요 시 "/health", healthcheckTimeout 90 추가)
